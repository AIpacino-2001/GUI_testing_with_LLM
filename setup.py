from setuptools import setup

setup(
   name='GTestAgent',
   version='1.0',
   description='A GTestAgent module for better GUI testing',
   author='greenmon',
   author_email='greenmon@kaist.ac.kr',
   packages=['GTestAgent'],
   install_requires=['openai'], 
)